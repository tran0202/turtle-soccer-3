import React from 'react'

class Knockout extends React.Component {
    render() {
        const { stage, config } = this.props
        const { groups, advancements } = stage
        return <React.Fragment>Knockout</React.Fragment>
    }
}

export default Knockout
